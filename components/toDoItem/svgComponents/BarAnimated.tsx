import React from 'react';
import { View, Animated, StyleSheet} from 'react-native'
import Svg, { Path, G } from "react-native-svg";

const BarAnimated = props => {

}

const styles = StyleSheet.create({

})

export default BarAnimated;